import React from "react";

import Page from "../../hoc/Aux/Page";

const geolocator = props => {
  return (
    <Page title="Geolocator" description="Find facilities below on the map." />
  );
};

export default geolocator;
